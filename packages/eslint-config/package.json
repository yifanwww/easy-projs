{
    "name": "@packages/eslint-config",
    "version": "1.0.0",
    "description": "ESLint configurations for easy-projs.",
    "homepage": "https://github.com/YF1999/easy-projs#readme",
    "license": "MIT",
    "author": {
        "name": "YF1999",
        "email": "1586939317@qq.com",
        "url": "https://github.com/YF1999"
    },
    "bugs": {
        "url": "https://github.com/YF1999/easy-projs/issues"
    },
    "private": true,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/YF1999/easy-projs.git",
        "directory": "packages/eslint-config"
    },
    "scripts": {
        "build": "echo NPM script `build` does nothing in `@packages/eslint-config`",
        "build-scripts": "echo NPM script `build-scripts` does nothing in `@packages/eslint-config`",
        "clean": "echo NPM script `clean` does nothing in `@packages/eslint-config`",
        "clean-scripts": "echo NPM script `clean-scripts` does nothing in `@packages/eslint-config`",
        "format": "prettier --write **/*.{cjs,css,html,js,jsx,mjs,json,scss,ts,tsx,yaml,yml}",
        "lint": "echo NPM script `lint` does nothing in `@packages/eslint-config`",
        "test-coverage": "echo NPM script `test-coverage` does nothing in `@packages/eslint-config`"
    }
}
