{
    "extends": "@easy/eslint-config",
    "ignorePatterns": ["*.cjs", "*.js", "*.mjs", "dist", "lib", "lib-commonjs"],
    "parserOptions": {
        "project": "./tsconfig.json"
    },
    "overrides": [
        {
            "files": "src/**/*.ts",
            "rules": {
                "no-console": ["error", {}]
            }
        }
    ]
}
