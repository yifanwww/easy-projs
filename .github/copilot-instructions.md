# Copilot Instructions

## Tooling

- Use `rg` (ripgrep) for all code search operations — finding files, grepping for patterns, and
  locating references. Do not use `grep`, `findstr`, the built-in `grep_search`, or `file_search`
  tools.
